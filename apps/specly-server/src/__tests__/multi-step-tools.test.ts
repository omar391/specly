import { describe, it, expect } from 'vitest';
// Placeholder for removed multi-step tool integration tests.
describe('legacy multi-step tools placeholder', () => {
	it('should assert multi-step tooling removed', () => {
		expect(true).toBe(true);
	});
});
