// Dynamic schema generator removed during Specly migration.
// Legacy multi-step tool flow based dynamic enum generation no longer applies.
// Stub retained intentionally to avoid breaking imports; will be removed after full cleanup.
export {};
